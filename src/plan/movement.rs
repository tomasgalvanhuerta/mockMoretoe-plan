#[derive(Default)]
pub struct Movement {
    pub author: String,
    pub explanation: String,
    pub guid: String,
    pub index: i32,
    pub isAuthor: bool,
    pub name: String,
    pub standardTimer: i32,
}

impl Movement {}
