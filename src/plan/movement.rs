#[derive(Default)]
struct Movement {
    author: String,
    explanation: String,
    guid: String,
    index: i32,
    isAuthor: bool,
    name: String,
    standardTimer: i32,
}

impl Movement {
    
}